# Desktop shell

This package is intentionally a placeholder. The Electron shell will wrap the Expo web export
and provide native file-system integration after the first writing workflow exists. It does not
own product logic; that belongs in `packages/core`.
